# Drone Navigation API — FastAPI backend with fastapi-users
fastapi>=0.110
uvicorn[standard]>=0.29

# fastapi-users with the SQLAlchemy adapter (FastAPIUsers, JWT strategy, OAuth routers)
fastapi-users[sqlalchemy]>=12.1

# Database drivers
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29        # PostgreSQL (production, on the Caddy ECS)
aiosqlite>=0.19      # optional: local dev fallback via sqlite+aiosqlite URL

# Email verification / password reset (any SMTP provider: Alibaba DirectMail, Gmail, ...)
aiosmtplib>=3.0
email-validator>=2.0

# OAuth providers (Google now; Facebook/GitHub/Instagram later)
httpx-oauth>=0.13

# Synapse Admin API client (hidden chat-account provisioning, token brokering)
httpx>=0.27

# Task3 customer reports: render submitted drone photos and telemetry into PDF.
reportlab>=4.0
Pillow>=10.0
