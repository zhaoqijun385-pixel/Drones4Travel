# Drone Navigation 项目创新点、不足与演进路线

> 用途：作为项目汇报、产品介绍和 PPT 生成的事实源文档。  
> 资料口径：2026-08-06，`twin` 分支，基于原有 Drone Navigation 单机项目的当前工作树。  
> 重要说明：当前工作树包含尚未提交的增量改动；本文将“代码已实现”“自动化验证通过”“浏览器演示通过”和“真实飞行已验证”严格区分。

---

## 0. 一句话定位

Drone Navigation 是一个面向低空无人机任务控制的多视角飞行工作台：在保留原有单机飞行、2D 地图、Cesium 3D 场景、实时视频和 Crazyflie bridge 的基础上，进一步扩展为可搜索、可选机、可跟随、可规划任务、可联动 OpenClaw 的多无人机协同控制界面。

当前阶段最适合的产品表述是：

> “面向低空机群的可视化飞行甲板与仿真验证平台，正在从单机网页遥控逐步演进为带身份、任务、视角和安全边界的多无人机控制系统。”

不要把当前系统表述成已经完成的真实多机自主飞行系统。真实飞行仍需要独立的定位、控制权、链路隔离、服务端权威状态和现场安全验收。

---

## 1. 原本最开始的项目是什么

### 1.1 原始产品能力

项目最开始是一套以单架无人机为中心的网页飞行控制界面，主要能力包括：

- 通过 Vue 3 + Vite 构建浏览器端飞行控制台。
- 使用两个摇杆/控制盘完成飞行、偏航、云台和相机控制。
- 使用 HUD 展示状态、高度、电量、姿态、相机和链路信息。
- 使用 Cesium 展示 3D 空中场景。
- 使用 Google Maps 实现 2D 地图、卫星地图和相关位置能力。
- 在低空或地面状态下使用 Google Street View 提供街景式观察。
- 通过 MediaMTX 和 WHEP 播放无人机或摄像头视频。
- 通过 FastAPI 提供认证、设置、遥测和服务端 API。
- 通过 Crazyflie bridge 对接 Crazyradio、Crazyflie 和 ESP32 AI-Deck 视频链路。
- 通过 OpenClaw 提供客服/对话式助手能力。

原项目的优势是已经具备较完整的单机技术链路，但核心状态和交互主要围绕“当前这一架无人机”设计。

### 1.2 原始技术架构

| 层级 | 原始实现 | 作用 |
|---|---|---|
| 前端 | Vue 3、Vite、Vue Router、Vue I18n | 页面、状态和交互控制 |
| 三维场景 | Cesium、Google Photorealistic 3D Tiles、OSM fallback | 3D 地形、建筑和空中视角 |
| 二维地图 | Google Maps JavaScript API | 位置、路径、态势查看 |
| 地面观察 | Google Street View | 单机低空/地面观察体验 |
| 后端 | FastAPI、SQLAlchemy、PostgreSQL/SQLite 兼容驱动 | 用户、设置、流配置、遥测和服务端 API |
| 实时视频 | MediaMTX、WHIP/WHEP | 摄像头和无人机视频接入 |
| 飞控桥接 | Crazyflie bridge、cflib、WebSocket | 真实无人机命令和遥测转发 |
| 智能助手 | OpenClaw Gateway | 对话、态势同步和后续指令入口 |
| 本地运行 | Windows + WSL2、macOS、Ubuntu 文档 | 跨平台开发和部署 |

### 1.3 最开始暴露出的主要问题

这些问题来自实际使用反馈，而不是单纯的代码风格问题：

| 初始问题 | 表现 | 影响 |
|---|---|---|
| 视角不同步 | 左上角小地图在动，但主 3D 视角没有同步移动 | 用户无法确认正在观察哪一架飞机 |
| 高度控制不自然 | 高度变化速率固定，摇杆轻推和满推的手感差不多 | 无法精细悬停，也无法快速完成升降 |
| 下降过慢 | 下降速率和上升速率体验接近，低空收尾拖沓 | 降落和紧急回收效率低 |
| 远端无人机受本机地面逻辑影响 | 切换其他无人机后移动会被吸回地面、抽搐 | 多机控制对象之间发生状态串扰 |
| 非 FPV 视角被街景替代 | 跟随、俯视等视角可能切到 Google Street View | 用户失去空中观察感，且产生额外 WebGL/网络负担 |
| 3D 飞机过于简单 | 近似单一几何体或点标记，只有某个角度比较像飞机 | 不适合展示方向、机头、旋翼和任务状态 |
| 单机心智模型 | UI、相机、地面判断、命令路径都默认当前机是本机 | 无法安全扩展到多架独立无人机 |

---

## 2. 总体演进：从单机控制台到多机飞行甲板

| 维度 | 原始状态 | 当前增量状态 | 形成的价值 |
|---|---|---|---|
| 控制对象 | 当前本机 | 本机 + 多架带身份的无人机 | 可以切换、搜索和观察不同飞机 |
| 状态模型 | 单机位置、高度、姿态 | `droneId`、`local`、`phase`、`battery`、`online`、`sequence`、路线和任务 | 为多机状态同步和服务端权威化铺路 |
| 视角 | 单一主视角 | 自由、FPV、跟随、俯视、全览 | 观察视角与操作对象解耦 |
| 高度 | 固定灵敏度、共享地面判断 | 摇杆位移映射速率、每机相对高度、独立远端控制 | 多机操作不再共享错误的本机地面状态 |
| 任务 | 手动操作为主 | 前往、集合、分区巡检、交叉换位、返航 | 从“看飞机”升级为“管理任务” |
| 3D 表现 | 点或简化模型 | 多部件四旋翼 GLTF + 远距离点降级 | 方向、结构和身份更容易辨认 |
| 智能助手 | 通用 OpenClaw 入口 | 目标机选择、只读态势上下文、确认式快捷命令 | 为每机 Agent 和任务助手留出边界 |
| 运行方式 | 手工配置 | Windows/WSL2 交付脚本、锁文件安装、旧环境复用 | 降低团队接入成本 |

---

## 3. 核心创新点详解

### 3.1 在原有架构上增加可选机群层，而不是重写单机系统

**创新内容**

- 多机工作台作为现有单机 AerialView 的增量层接入。
- 普通入口仍可保持单机体验。
- `?fleet=demo` 作为演示/压力入口，可以生成本机加 20 架模拟无人机。
- 通过 `fleetEnabled`、`fleet.getRenderStates()` 和 Cesium fleet entity 层，把机群状态与原有单机状态分开管理。

**工程价值**

- 降低迁移风险：原有单机控制、实机链路和页面结构不需要一次性重写。
- 支持渐进式验证：先做浏览器仿真，再接服务端，再接真实飞控。
- 出现机群模块问题时，可以关闭机群模式回到单机排障路径。

**事实边界**

这是一种兼容式扩展架构，不等于已经完成服务端多机集群。当前演示机群的主要状态仍在浏览器内生成和维护。

### 3.2 用统一的无人机身份模型连接所有视图

**创新内容**

每架无人机不再只是一个坐标点，而是带有独立身份和状态的实体，核心字段包括：

- `droneId`：稳定的无人机身份。
- `name`：面向人的显示名称。
- `local`：是否为本机。
- `lat`、`lon`、`alt`：绝对地理位置/海拔表达。
- `x`、`y`、`z`：演示机群的相对位置/高度表达。
- `yaw`/`heading`：机体方向。
- `phase`：停机、起飞、悬停、航行、降落等状态。
- `battery`、`online`：链路和电量状态。
- `route`、`missionTarget`、`missionProgress`：任务上下文。
- `sequence`：为后续有序遥测同步预留的状态序号。

同一个 `selectedDroneId` 被多个模块共享：

```text
机群列表
    ↓
选中无人机状态
    ├── Cesium 3D 实体
    ├── 2D 地图 Marker
    ├── FPV / 跟随 / 俯视 / 全览相机
    ├── HUD 状态栏
    ├── OpenClaw 目标机
    └── 飞行甲板任务按钮
```

**工程价值**

- 避免 3D、2D、HUD 和 OpenClaw 各自维护一套“当前飞机”。
- 点击机群列表、3D 模型或 2D Marker 后，最终都会收敛到同一个选择状态。
- 为后续服务端的 `roomId + droneId + sequence` 增量协议提供前端承载结构。

### 3.3 将“操作对象”和“观察视角”解耦

**创新内容**

当前提供五种主要观察模式：

| 模式 | 行为 |
|---|---|
| 自由 Free | 相机完全交给用户，不持续跟随某架飞机 |
| FPV | 相机位于选中无人机附近，使用机体方向和云台姿态 |
| 跟随 Follow | 从选中机后上方保持距离跟随 |
| 俯视 Top | 从选中机正上方观察位置和方向 |
| 全览 Overview | 根据全机包围范围自动取景 |

同时，“选中哪架飞机”和“从什么角度观察”不再是同一件事：

- 选中远端飞机不会强制把用户从自由视角抢回去。
- 只有用户点击跟随、定位、俯视或全览时才改变相机。
- 切换对象时会立即更新相机目标，取消旧的 Cesium flight，避免小地图已经变化而主视角仍停在旧目标。

**解决的问题**

这直接针对“左上角 2D 小地图在动，但实际视角没有动”的反馈。问题的根因不是地图本身，而是不同视图对当前对象和相机控制权的定义不一致。

### 3.4 用相对高度与绝对海拔分离解决多机地面串扰

**创新内容**

演示机群同时保留两类高度：

- `z`：相对起飞/地面高度，用于每架飞机自己的飞行控制。
- `alt`：用于 Cesium 地理位置的绝对海拔表达。
- `surfaceAlt`：局部地面或采样表面的高度原点。

演示机的绝对高度遵循：

```text
alt = altitudeOrigin + z
```

远端无人机的手动控制不再进入本机的 `altitudeGate`。本机地面采样只负责本机，不会把远端无人机强制拉回本机地面。

**解决的问题**

- 切换其他无人机后移动被吸回地面。
- 远端飞机上下移动时抽搐。
- `z` 和 `alt` 在不同命令路径中不同步。
- 低空飞行被错误判断为已经落地。

**工程价值**

这是多机控制从“共享一个飞行状态”走向“每架飞机有独立飞行状态”的关键一步。

### 3.5 高度摇杆从固定速率改为按摇杆位移动态变速

**创新内容**

高度输入经过死区和非线性映射：

- 轻推：低速、适合微调。
- 中推：中速、适合普通爬升/下降。
- 满推：达到当前方向的最大速率。
- 中心死区：避免摇杆噪声造成高度漂移。

当前自动化测试明确覆盖：

- `0` 和小于死区的输入不改变高度。
- `0.25` 的输入速度低于 `0.75`。
- 上升手动控制上限为约 `3 m/s`。
- 当前下降方向允许更高的最大速率，测试值为 `-8 m/s`，用于改善用户反馈中的下降过慢问题。
- 自动起飞/降落演示状态机使用约 `8 m/s` 的连续过渡速度。

**工程价值**

高度控制从“开关式控制”变成了更接近真实遥杆的连续控制，既能精调也能快速回收。

**仍需验证**

这些数值目前主要经过浏览器物理测试和仿真验证。真实 Crazyflie 的遥测延迟、姿态稳定器、负载和电池状态仍可能改变实际手感，需要在地面测试后再调参。

### 3.6 机群模式下主动避免 Street View 接管空中视角

**创新内容**

当前机群工作台的 FPV、跟随、俯视和全览都保持在 Cesium 3D 场景中，不让选中远端飞机因为低高度触发本机的 Street View 地面逻辑。

代码层面的策略是：

- `fleetEnabled` 时禁用机群工作台的 Street View 切换。
- 机群相机统一使用 Cesium 的世界坐标和目标相机。
- 只有原始单机路径仍保留原有的低空 Street View 兼容行为。
- 选中远端飞机时，Street View POV 不再错误使用本机的经纬度和高度。

**解决的问题**

这直接回应“FPV 是高空飞行感觉，但新建无人机 takeoff 或切换视角后变成地图街景”的反馈。

**重要边界**

目前不是全局永久删除 Street View。为了不破坏原项目的单机体验，非机群模式下的原始 Street View 逻辑仍然存在。如果产品目标是“任何页面都禁止街景调用”，下一阶段需要增加全局功能开关并彻底替换单机低空背景策略。

### 3.7 从点标记升级为可从多视角识别的四旋翼模型

**创新内容**

原始 3D 表现偏向点或高度简化的单一几何体。当前改为可维护的程序化 GLTF 资产，生成脚本位于：

```text
scripts/generate-fleet-drone-model.mjs
```

当前模型结构包括：

- 多面体主机身。
- 浅色顶部装甲壳。
- 两条交叉碳纤维机臂。
- 四个电机舱和四个旋翼中心。
- 四组双叶旋翼。
- 四组起落架和两条起落滑橇。
- 下置相机球形罩和镜头。
- 顶部天线/桅杆。
- 橙色机头方向标识。
- 石墨、陶瓷、青色、橙色、碳纤维和玻璃等多种材质。

当前 GLTF 资产约为：

- 27 个节点。
- 12 个网格。
- 6 个材质。
- 内嵌二进制缓冲区，不依赖额外纹理文件。

另外增加了统一的 Z-up 到 GLTF Y-up 坐标转换根节点，解决模型“侧放”后俯视只剩细线的问题。

**多视角价值**

| 视角 | 可识别信息 |
|---|---|
| 俯视 | X 型机臂、四个旋翼、机头方向 |
| 跟随 | 机身、起落架、下置相机、顶部结构 |
| 侧视/低角度 | 机身高度、相机球、起落结构 |
| 远距离 | 退化为带身份颜色的点标记，保持性能 |

这不是追求影视级写实，而是优先保证“方向可读、结构可读、身份可读、远近可用”。

### 3.8 任务从单机按钮升级为机群任务状态机

当前演示任务包括：

- 单机前往目标。
- 一键集合。
- 分区巡检。
- 交叉换位。
- 全部返航。

任务状态不再只显示一个“执行中”，而是保留：

```text
停机 → 起飞 → 航行 → 到达/驻留 → 返航 → 降落
```

每架飞机拥有自己的：

- 当前阶段。
- 任务目标。
- 任务进度。
- 路线点。
- 分配的停位。
- 任务完成状态。

为了让演示更稳定，当前采用保守策略：

- 集合任务为无人机分配分离位置。
- 路线和目标不直接重合到同一个坐标。
- 测试中保持至少 3 米动态间隔。
- 低空走廊按顺序放行，牺牲吞吐量换取可验证性。

**创新价值**

这使系统从“多个图标同时移动”升级为“每架飞机有自己的任务生命周期”。

### 3.9 3D/2D/HUD/视频/OpenClaw 的联动工作台

当前不是简单堆叠多个窗口，而是围绕同一机群状态做联动：

- 机群托盘：搜索、选中、删除、定位和查看状态。
- Cesium 3D：模型、路线、相机和全览。
- 2D 态势窗口：全部飞机或选中飞机的 Marker。
- HUD：选中飞机、状态、高度、电量、控制权、任务段。
- 实时视频：根据 `streamId`/`droneId` 查找对应流，找不到时回退主流。
- OpenClaw：选择目标机，发送只读态势上下文和确认式快捷命令。

组件之间通过统一选择状态和事件链路连接，而不是各自维护隐式选中对象。

### 3.10 OpenClaw 的“助手”与“飞控命令”边界

当前 OpenClaw 联动的设计重点不是让模型直接操纵飞控，而是建立安全边界：

- 支持按目标无人机选择上下文。
- 支持发送房间、位置、电量、选中机和目标等只读态势信息。
- 快捷飞行命令在 UI 侧需要二次确认。
- 演示模式下快捷命令只改变浏览器模拟状态。
- 真实模式需要经过现有控制权租约、服务端命令白名单和会话链路。

**创新价值**

把大模型从“直接控制器”定位为“态势助手 + 受控指令入口”，避免通过聊天旁路绕过飞控安全策略。

### 3.11 性能不是最后再处理，而是和功能一起设计

当前已经加入多项轻量化策略：

- Cesium Entity 复用，而不是每个遥测包都新建实体。
- 机群渲染更新上限约 20 Hz。
- 相机同步上限约 30 Hz。
- 2D 机群位置刷新约 5 Hz。
- 按距离显示 3D 模型，远距离退化为点。
- 标签比模型更早隐藏，减少文字开销。
- 选中状态、颜色和标签尽量增量更新。
- Google 地图、视频和态势窗口尽量保留实例，减少重复初始化。
- 3D Tiles 首屏先放行可操作粗场景，再在后台细化。

**当前可量化口径**

- `?fleet=demo` 浏览器演示覆盖 21 架无人机。
- 正常工作距离内使用 GLTF，约 2.5 千米外使用点标记降级。
- 机群状态更新限制为 20 Hz。
- 2D 态势窗口更新限制为 5 Hz。
- 当前未完成 50/100 架机群、低端移动设备和真实网络抖动下的完整基准测试。

### 3.12 面向 Windows 团队协作的交付改造

当前增加了：

- `WINDOWS-QUICKSTART-zh-CN.md`。
- `scripts/windows/Setup-DroneNavigation.ps1`。
- `scripts/windows/Start-DroneNavigationDemo.ps1`。
- `client/config.example.json`。
- `COLLABORATION.md`。

Windows 交付脚本的目标是保持原项目的 WSL2 + Ubuntu + conda + Node 组织方式：

- 不把 macOS 的 `node_modules` 塞进压缩包。
- 优先复用接收者已有的 `~/drone-navigation` 环境。
- 锁文件一致时复用已有 `node_modules`。
- 复用已有本地配置，但不把配置打进公共包。
- 没有旧环境时才按锁文件和 requirements 安装。

这不是新建一套平行工程，而是把现有项目的环境启动路径变得更容易交接。

---

## 4. 用户最初反馈与当前解决状态

| 用户反馈 | 当前处理 | 当前状态 |
|---|---|---|
| 左上角 2D 小地图在动，但主视角不动 | 统一 `selectedDroneId`；切换对象后立即调用 Cesium 相机更新；取消旧的飞行动画；分离选择和观察模式 | 浏览器演示已验证；真实遥测方向仍需现场验证 |
| 无法自由调整无人机高度 | 远端演示机接入共享飞行盘；每架机独立维护 `z/alt` 和自动目标高度 | 仿真可用；真实多机命令链尚未完成 |
| 高度变化速率恒定 | 根据遥杆位移、死区和方向映射高度速度 | 自动化测试通过；实际飞行参数仍需调参 |
| 下降速度太慢 | 当前下降方向使用更高速度上限，自动降落保持连续状态机 | 仿真测试通过；真实硬件响应未验证 |
| 其他无人机移动会回地面、抽搐 | 远端选中机绕过本机 `altitudeGate`，使用独立手动飞行状态 | 自动化测试覆盖；实时无人机仍依赖后端状态 |
| 非 FPV 视角变成 Google 街景 | 机群工作台禁用 Street View，FPV/跟随/俯视/全览都留在 Cesium | 机群模式已处理；原始单机模式仍保留街景兼容逻辑 |
| 飞机模型过于单一、单一角度才看得出 | 多部件 GLTF、统一坐标根节点、四旋翼结构、机头标识和多材质 | 俯视/跟随浏览器复核通过 |

---

## 5. 当前完成度分层

### 5.1 已实现并有代码/测试证据

- 多机工作台和机群身份模型。
- 21 架演示机的生成、选择、搜索和状态展示。
- Free、FPV、Follow、Top、Overview 五种相机模式。
- 远端演示机独立手动移动、旋转、起飞和降落。
- 高度摇杆动态速率映射和死区。
- 前往、集合、巡检、交叉换位和返航演示任务。
- 3D 模型/点标记距离降级。
- 机群模式禁用 Street View 地面接管。
- 多部件无人机 GLTF 与可复现生成脚本。
- OpenClaw 目标机选择和确认式飞行助手边界。
- Windows/WSL2 交付脚本和安全配置模板。

### 5.2 已通过自动化或浏览器验证

- 当前 `npm run test:fleet`：14 项通过。
- 当前 `npm run build`：构建成功。
- GLTF JSON、节点、网格、材质和内嵌缓冲区校验通过。
- 浏览器中可以看到 21 架演示机。
- 俯视能看到完整 X 型机臂和四个旋翼。
- 跟随视角能看到机身、起落架、相机和橙色机头标识。
- 最终模型页面复核未出现新的 Cesium error。

### 5.3 仍然不能宣称已经完成

- 真实多架 Crazyflie 同时受控飞行。
- 多用户之间的服务端权威机群状态同步。
- 可靠定位、室内定位或 GPS 丢失情况下的自主飞行。
- 真实链路拥塞、丢包、延迟和失联恢复。
- 真实电池、风场、载荷和电机性能下的高度参数标定。
- 经过现场安全审批的自动前往、集合和巡检。
- 50/100 架无人机和移动端设备的性能验收。

---

## 6. 当前不足与风险

### 6.1 真实多机后端还没有完全闭环

**现状**

- 演示机群主要由浏览器本地状态机驱动。
- 当前任务规划和远端手动操作主要作用于 demo 状态。
- 真实无人机仍需经过 Crazyflie bridge、服务端命令白名单和控制权租约。

**缺口**

- 服务端需要成为位置、状态、任务和控制权的权威来源。
- 需要完整的 `roomId + droneId + sequence` 增量状态协议。
- 需要处理乱序包、重复包、断线重连和多客户端冲突。
- 需要每架无人机独立的控制租约、心跳和超时回收。

**优先级**：P0/P1。没有这部分，不能把浏览器演示升级为真实多机协作。

### 6.2 真实飞行安全链路仍需现场验收

当前系统虽然保留了控制权、命令白名单和桥接层，但仍不能替代：

- 实体无人机身份核验。
- Crazyradio 信道和地址隔离。
- USB/无线链路稳定性测试。
- 室内或室外定位有效性。
- 地理围栏和禁飞区。
- 失联、低电量、姿态异常和电机异常处理。
- 人工急停和安全观察员流程。

需要强调：能在浏览器中移动 21 个模拟模型，不代表 21 架真实无人机可以安全飞行。

### 6.3 Street View 仍未从整个产品中彻底移除

机群工作台已经避免街景接管，但原始单机路径仍保留低空 Street View，以保持旧功能兼容。

如果产品定位最终确定为“极力避免街景调用”，还需要：

1. 增加全局 `disableStreetView` 配置。
2. 默认所有空中/地面视角都留在 Cesium。
3. 以 Cesium 地形、OSM Buildings 或自有低空场景替代原始街景背景。
4. 明确 2D Google Map、Google 3D Tiles 和 Google Street View 三者的调用边界。

### 6.4 高度模型仍然是“相对高度 + 地形高度”的过渡方案

当前用 `surfaceAlt + z` 解决了演示中的基本高度同步，但真实场景还会受到影响：

- Google 3D Tiles 地形采样可能延迟。
- 不同数据源的地面高度定义不一致。
- 起飞点和目标点之间地面高度不同。
- 远端无人机需要自己的地面参考，而不是共享本机参考。
- 真实控制器可能以姿态、垂直速度或气压高度为闭环输入。

下一步需要把“地面参考、目标相对高度、绝对海拔、控制器反馈高度”分成明确的四层。

### 6.5 小地图方向已经统一，但尚未完成真实遥测校准

当前代码已经优先使用 `yaw`，并把选中机、Marker、3D orientation 和相机方向连接起来。但真实环境仍可能有：

- 机体坐标轴和地图北向定义不一致。
- `heading` 与 `yaw` 的来源不同。
- 磁力计/IMU/GPS 航向漂移。
- 2D 地图和 Cesium 世界坐标的角度正方向差异。
- 网络延迟导致位置和航向不在同一个序号。

需要用真实遥测回放文件和固定航向测试，确认“机头指向、2D 箭头、3D 机体朝向、FPV 画面”四者一致。

### 6.6 任务规划仍属于演示级策略

当前任务使用固定停位、简单短路径、低空走廊和间距规则，优点是容易解释和稳定演示，缺点是：

- 没有完整的三维障碍物规划。
- 没有动态避障和全局重规划。
- 没有电池/载荷/通信质量约束。
- 没有真实禁飞区、地理围栏和天气数据。
- 低空走廊按序放行会牺牲多机并行效率。
- “至少 3 米间距”是仿真几何约束，不是实体安全距离证明。

### 6.7 模型已经可读，但仍是轻量低多边形资产

当前模型重点是结构识别，不是工业级数字孪生：

- 没有真实无人机的尺寸、材质和细节标定。
- 旋翼没有真实动画和运动模糊。
- 没有按距离切换多级 LOD 网格。
- 没有真实灯光、螺旋桨透明度、相机云台运动和载荷接口。
- 各型号无人机仍共享一套基础外形，机型差异化不足。
- 当前材质使用轻量 GLTF/Unlit 思路，不能替代高质量 PBR 资产。

### 6.8 性能仍有工程债务

当前构建仍有已知 warning：

- Google Maps 模块同时被静态和动态引用，动态导入无法完全拆分。
- 部分前端 chunk 超过 500 kB。
- Google Marker API 已有弃用提示，后续应迁移到 Advanced Marker，并配置相应 map ID。
- 大量媒体资源会增加源码包体积。
- 还没有系统记录不同机数、不同 GPU、不同浏览器和不同网络下的 FPS/内存/首屏时间。

### 6.9 配置和安全仍偏开发环境

- Google Maps、Cesium ion 和 OpenClaw 配置在前端构建时读取，API key 必须做域名/API 限制。
- 服务端配置仍需要接收者自行填写数据库、SMTP、OAuth、Synapse 和 MediaMTX 参数。
- OpenClaw token、Synapse admin token 和真实无人机地址不能进入公共压缩包。
- 当前 Windows 交付脚本能降低安装成本，但完整后端仍依赖 WSL 服务、数据库、OpenClaw 和 MediaMTX 的本机准备。
- 当前压缩包是源码快照，不包含 Git 历史；长期多人协作仍应迁移到私有 Git 远端。

### 6.10 浏览器本地持久化还不是团队共享数据

机群档案和部分界面状态保存在浏览器 `localStorage`，适合演示和个人开发，但不适合：

- 多用户共同编辑机群。
- 多浏览器状态一致。
- 审计谁在什么时候控制了哪架飞机。
- 恢复服务端历史任务。
- 跨设备共享路线、目标和任务进度。

后续应迁移到服务端房间/任务模型，并保留本地缓存作为断网回退，而不是作为唯一事实来源。

---

## 7. 下一阶段路线图

### P0：先稳定当前体验

1. 完成 2D 航向和真实遥测回放校准。
2. 对高度控制做真实遥测闭环测试，分别标定上升、下降、悬停和降落。
3. 增加全局 Street View 禁用开关，明确默认行为。
4. 增加机群模式下的错误提示、断链提示和恢复提示。
5. 增加 21 架演示机的浏览器回归测试和截图验收。

### P1：建立真正的服务端机群状态

1. 设计 `roomId + droneId + sequence` 状态协议。
2. 服务端维护每架无人机的权威位置、状态、任务和在线状态。
3. 增加 WebSocket 增量广播、乱序丢弃和断线重连。
4. 将 `selectedDroneId` 与控制租约绑定，而不只是 UI 选择。
5. 增加任务创建、暂停、取消和恢复的服务端记录。

### P2：完成安全闭环

1. 每架无人机使用可验证的身份，而不只依靠颜色、信道或地址。
2. 实现控制租约、心跳、超时释放和紧急接管。
3. 加入地理围栏、最低/最高高度和禁飞区检查。
4. 增加低电量、定位失效、遥测停止和视频中断状态机。
5. 进行单机地面测试、单机低空测试，再进行多机隔离测试。

### P3：升级任务规划

1. 将固定停位替换为可配置任务区域和安全缓冲区。
2. 基于地形、建筑和禁飞区生成三维可行路径。
3. 加入电量、通信质量和载荷能力约束。
4. 实现动态避障和失败任务重规划。
5. 用仿真回放验证 5、20、50、100 架不同规模。

### P4：升级产品体验和模型

1. 增加型号、载荷、相机云台和灯光状态的实体表达。
2. 为模型增加旋翼动画和多级 LOD。
3. 迁移 Google Advanced Marker，整理地图依赖拆分。
4. 对移动端和低端 GPU 进行专项优化。
5. 提供任务回放、轨迹导出、截图和报告生成。

---

## 8. 建议 PPT 结构

下面的结构可以直接交给 PPT 生成器，也可以压缩成 10 页以内的版本。

### 第 1 页：项目标题

**标题**：Drone Navigation：从单机网页遥控到多无人机飞行甲板  
**副标题**：多视角、任务化、可验证、面向真实飞控演进

画面建议：使用 3D 机群俯视图，叠加深色飞行甲板 UI。

### 第 2 页：原始项目基础

讲清楚项目不是从零开始：

- Vue 3 + Vite 前端。
- Cesium 3D 场景。
- Google 2D Map/Street View。
- FastAPI + PostgreSQL。
- MediaMTX 视频。
- Crazyflie bridge。
- OpenClaw。

核心叙事：原项目已经具备完整单机链路，我们做的是在不破坏原链路的前提下扩展多机能力。

### 第 3 页：最初问题

用用户实际反馈作为问题页：

- 小地图动、主视角不动。
- 高度速率固定且下降慢。
- 远端飞机被本机地面逻辑拉回。
- 非 FPV 视角变成街景。
- 飞机模型单一，只从一个角度可见。

### 第 4 页：总体架构演进

展示：

```text
原有单机控制链路
        ↓ 增量扩展
统一 Fleet State
        ├── 3D Cesium
        ├── 2D Map
        ├── FPV / Follow / Top / Overview
        ├── HUD / Fleet Tray
        ├── Mission Planner
        └── OpenClaw Assistant
```

强调：不是并行搓一个 demo，而是接入原项目。

### 第 5 页：创新一——多机身份与统一选择状态

要点：

- `droneId`、相对/绝对高度、任务、在线和电量。
- 列表、3D 模型、2D Marker、HUD、OpenClaw 共用选择状态。
- 从“一个当前无人机”变为“可管理的无人机实体集合”。

### 第 6 页：创新二——多视角与空中观察策略

要点：

- Free、FPV、Follow、Top、Overview。
- 操作对象和观察视角解耦。
- 机群模式避免 Street View 接管空中镜头。
- 解决主视角和小地图状态不同步。

### 第 7 页：创新三——独立高度与动态控制

要点：

- `z` 相对高度与 `alt` 绝对海拔分离。
- 摇杆位移映射高度速度。
- 远端无人机绕过本机地面门控。
- 自动起降采用连续状态机。

### 第 8 页：创新四——任务化机群

要点：

- 前往、集合、巡检、交叉换位、返航。
- 每架机有独立路线、阶段和进度。
- 3 米间隔和保守低空走廊。
- 先仿真验证，再接真实控制链。

### 第 9 页：创新五——可识别的 3D 无人机

要点：

- 多部件四旋翼，而不是单一方块或点。
- 四旋翼、起落架、相机、天线和机头方向标识。
- 多材质、身份色轻度混合、远距离降级。
- 俯视和跟随视角都能识别。

### 第 10 页：创新六——安全边界和 OpenClaw

要点：

- OpenClaw 是态势助手和确认式入口。
- 不允许聊天旁路绕过控制租约和命令白名单。
- 演示状态和真实飞控链路分开。
- 真实多机飞行仍需要身份、租约、定位和现场验收。

### 第 11 页：验证结果

可放入的事实数据：

- 21 架浏览器演示机。
- 14 项机群/飞行物理测试通过。
- 前端生产构建通过。
- 3D 模型 27 节点、12 网格、6 材质。
- 3D/2D/HUD/相机联动已完成浏览器检查。
- Windows/WSL2 交付包已生成。

### 第 12 页：仍然不足

用四个大类概括：

1. 真正的多机后端和权威状态还没闭环。
2. 真实飞行安全和定位还未验收。
3. Street View 尚未从整个产品全局移除。
4. 性能、模型和任务规划仍属于演进版本。

### 第 13 页：路线图

```text
当前演示可靠性
        ↓
服务端权威机群状态
        ↓
控制租约与安全闭环
        ↓
三维任务规划与真实定位
        ↓
多机现场飞行验收
```

### 第 14 页：结论

建议结论文案：

> 本项目的核心创新不只是“页面上出现了更多无人机”，而是把单机控制系统拆解为可扩展的身份、状态、视角、任务和安全边界，并在原有项目环境中完成可验证的渐进式升级。下一阶段的重点不是继续堆 UI，而是把浏览器仿真能力迁移成服务端权威状态和可审计的真实飞行控制闭环。

---

## 9. 建议现场演示流程

### 演示前

1. 运行 `?fleet=demo`。
2. 确认机群面板显示 21 架演示机。
3. 先使用俯视视角展示全机分布。
4. 选择一架远端飞机，展示名称、电量、状态和目标。

### 演示步骤

1. 点击 `Top`：展示多机分布和方向。
2. 点击 `Follow`：展示选中机的机身、旋翼、起落架和相机。
3. 点击 `FPV`：展示空中第一人称，不触发街景。
4. 拖动高度摇杆：轻推时慢速，满推时快速。
5. 切换另一架远端机：展示选中状态和视角联动。
6. 对远端机执行起飞/移动：展示它不会被本机地面逻辑拉回。
7. 执行集合或分区巡检：展示路线、阶段和进度。
8. 打开 2D 态势窗口：展示 2D Marker 与 3D 目标联动。
9. 打开 OpenClaw：展示目标机选择和确认式命令入口。
10. 展示模型生成脚本和 GLTF 结构，说明资产可重复生成。

### 演示时不要说

- “已经可以同时安全控制 21 架真实无人机”。
- “所有页面都已经取消 Google Street View”。
- “3 米间距已经证明真实飞行安全”。
- “OpenClaw 可以直接自主驾驶无人机”。
- “当前模型就是实际硬件的工业级数字孪生”。

### 建议说法

- “21 架是浏览器演示规模，真实多机需要服务端和硬件验收。”
- “机群工作台已经避免街景接管，但原始单机兼容路径仍保留。”
- “3 米是仿真任务的几何约束，不是现场安全距离结论。”
- “OpenClaw 当前负责态势和确认式入口，不绕过飞控安全链。”
- “当前模型优先解决方向和结构识别，后续再增加真实型号细节。”

---

## 10. 证据和源码索引

| 内容 | 主要文件 |
|---|---|
| 多机状态、档案、任务和演示命令 | `client/composables/useDroneFleet.js` |
| 远端手动飞行与相对高度 | `client/composables/fleetManualFlight.js` |
| 高度摇杆动态速率 | `client/composables/flightAltitudeMath.js`、`client/composables/useFlightPhysics.js` |
| 机群 UI、相机模式和 Street View 策略 | `client/src/views/AerialView.vue` |
| Cesium 模型、实体、点降级和相机 | `client/src/cesium-main.js` |
| 3D 无人机生成器 | `scripts/generate-fleet-drone-model.mjs` |
| 3D 无人机资产 | `client/public/models/fleet-drone.gltf` |
| 机群/飞行物理自动化测试 | `client/tests/fleetManualFlight.test.js`、`client/tests/controllerPhysics.test.js` |
| 原始多机阶段记录 | `setup-notes/08-multi-drone-foundation.md` 至 `setup-notes/18-sql-openclaw-login-hardening.md` |
| 最近的注册、生命周期和 FPV 修正 | `setup-notes/19-registration-backend-recovery.md` 至 `setup-notes/22-fpv-takeoff-and-camera-transition-fix.md` |
| Windows/WSL2 交付 | `WINDOWS-QUICKSTART-zh-CN.md`、`scripts/windows/` |
| 原始平台环境 | `README-zh.md`、`README-macos-zh.md`、`README-ubuntu-zh.md` |

### 可复现验证命令

```bash
cd client
npm run test:fleet
npm run build
git diff --check
```

GLTF 结构检查示例：

```bash
jq -e '.asset.version == "2.0" and (.nodes|length >= 20) and (.meshes|length >= 10)' \
  client/public/models/fleet-drone.gltf
```

---

## 11. 最终总结

### 已经形成的创新闭环

```text
单机网页飞行控制
        ↓
多无人机身份与状态
        ↓
多视角统一相机
        ↓
独立高度与动态控制
        ↓
任务状态机和空间间隔
        ↓
可识别的 3D 机体
        ↓
OpenClaw 受控助手
        ↓
Windows/WSL2 可交付环境
```

### 还没有形成的闭环

```text
浏览器模拟状态
        ↓ 仍需建设
服务端权威状态
        ↓ 仍需建设
身份认证与控制租约
        ↓ 仍需建设
可靠定位、失联恢复和地理围栏
        ↓ 仍需建设
真实多机现场安全验收
```

项目当前最有价值的成果，是已经把“多无人机”从视觉层面的多个图标，推进到了身份、状态、视角、任务和安全边界的系统化模型；项目当前最大的不足，则是这些能力还主要停留在浏览器仿真和单机基础设施之上，必须通过服务端权威化、真实链路验证和分阶段现场测试，才能成为真正可用的多无人机控制系统。
